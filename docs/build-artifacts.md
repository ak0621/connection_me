# Build Artifacts

This project uses one C++20/CMake codebase for macOS, Windows, and Linux. The executable protocol and config format are shared, so binaries built from the same commit can interconnect across the LAN.

## Current Host Output

This host is Ubuntu Linux x86_64. The locally built executable is:

```text
/home/akun/work/my_barrier/build/mybarrier
```

Packaged Linux outputs generated on this host:

```text
/home/akun/work/my_barrier/dist/mybarrier-linux-x86_64/mybarrier
/home/akun/work/my_barrier/dist/mybarrier-linux-x86_64.tar.gz
/home/akun/work/my_barrier/build/mybarrier-0.2.0-Linux-x86_64.deb
/home/akun/work/my_barrier/build/mybarrier-0.2.0-Linux-x86_64.tar.gz
/home/akun/work/my_barrier/build/mybarrier-0.2.0-Linux-x86_64.zip
```

These are Linux x86_64 artifacts. They will not run on macOS or Windows.

## Platform Build Commands

Linux/macOS:

```bash
scripts/build.sh
scripts/package-current.sh
```

Windows PowerShell:

```powershell
.\scripts\build.ps1
.\scripts\package-current.ps1
```

Expected executable outputs:

| Platform | CLI executable | Desktop UI when Qt is available |
|----------|----------------|-------------------------------|
| Linux | `build/mybarrier` | `build/MyBarrier` |
| macOS | `build/mybarrier` | `build/MyBarrier.app` |
| Windows Visual Studio generator | `build/Release/mybarrier.exe` | `build/Release/MyBarrier.exe` |
| Windows single-config generator, such as Ninja | `build/mybarrier.exe` | `build/MyBarrier.exe` |

Expected package and installer outputs:

| Platform | Runnable package | Installer package |
|----------|------------------|-------------------|
| Linux | `dist/mybarrier-linux-x86_64/`, `dist/mybarrier-linux-x86_64.tar.gz`, `build/mybarrier-*-Linux-*.tar.gz`, `build/mybarrier-*-Linux-*.zip` | `build/mybarrier-*-Linux-*.deb` |
| macOS with Qt UI | `dist/mybarrier-macos-arm64/` or `dist/mybarrier-macos-x86_64/`, `build/mybarrier-*-Darwin-*.tar.gz`, `build/mybarrier-*-Darwin-*.zip` | `build/mybarrier-*-Darwin-*.dmg` containing `MyBarrier.app` |
| macOS CLI-only | `build/mybarrier-*-Darwin-*.tar.gz`, `build/mybarrier-*-Darwin-*.zip` | no DMG |
| Windows | `dist/mybarrier-windows-x86_64/`, `dist/mybarrier-windows-x86_64.zip`, `build/mybarrier-*-Windows-*.zip` | `build/mybarrier-*-Windows-*.exe` |

Installer packages are generated by CPack:

```bash
cmake --build build --target package --config Release
```

The desktop UI is controlled by `MYBARRIER_BUILD_GUI`:

- `AUTO`: build the Qt UI if Qt 6 Widgets is found, otherwise build CLI only.
- `ON`: require Qt 6 Widgets and fail configuration if it is missing.
- `OFF`: build CLI only.

For macOS, the DragNDrop DMG generator is enabled only when the Qt UI target exists. That keeps CLI-only builds from producing a misleading DMG with just `bin` and `share` folders.

On Windows, the NSIS installer generator requires NSIS to be installed. The GitHub Actions workflow installs it before packaging.

## CI Artifacts

The workflow `.github/workflows/build.yml` builds and uploads:

- `mybarrier-linux-x86_64`
- `mybarrier-macos-arm64`
- `mybarrier-windows-x86_64`

Each uploaded artifact contains the runnable CLI directory plus CPack packages from `build/mybarrier-*`. The workflow installs Qt and configures `-DMYBARRIER_BUILD_GUI=ON`, so the CPack packages include the desktop UI: Linux `MyBarrier`, macOS `MyBarrier.app`, and Windows `MyBarrier.exe`. Linux CI produces `.deb`, macOS CI produces `.dmg`, and Windows CI produces an NSIS `.exe` installer plus `.zip`.

A Linux machine cannot directly produce a native macOS executable without an Apple toolchain. Windows binaries can be cross-compiled in some setups, but this project uses Windows CI or a Windows machine as the reliable path.

## Runtime Interop

To interconnect devices, run the same-version `mybarrier` binary on each OS:

1. Start one daemon with `serve --pair-code <code>`.
2. Pair from another device with `pair --host <ip> --code <code>`.
3. Use `ping --peer <peer>` to verify the trusted peer connection.
4. Use clipboard and file commands across paired peers.

Example:

```bash
./mybarrier serve --name mac --role server --pair-code 123456
./mybarrier pair --host <mac-ip> --code 123456
./mybarrier ping --peer mac
```

Native keyboard/mouse sharing still requires platform input adapters. The current binary has the authorized input event channel and diagnostics, but native injection is not complete.
